# Các liên kết và thiết lập không nhạy cảm để tại đây.
app.name=TestPilot Studio
app.dataDir=data
app.outputDir=outputs
runner.headless=false
runner.slowMotionMs=120
runner.defaultTimeoutMs=15000
runner.maxParallel=2
preview.afterEachStep=true

# Bien duoc dung trong Excel: ${BASE_URL}
env.BASE_URL=https://example.com

# Không lưu mật khẩu vào file này. Dùng biến môi trường:
# TESTPILOT_USERNAME, TESTPILOT_PASSWORD
